<%= form_for(general_direction) do |f| %>
  <% if general_direction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(general_direction.errors.count, "error") %> prohibited this general_direction from being saved:</h2>

      <ul>
      <% general_direction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :direction_id %>
    <%= f.text_field :direction_id %>
  </div>

  <div class="field">
    <%= f.label :department_id %>
    <%= f.text_field :department_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
