<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-header">
    <div class="logo_lapiz">  
      <%=image_tag("navigation/logoLapiz.png", alt:"SiPTeD", size:"40")%>
    </div>
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
  </div>

  <div class="navbar-collapse collapse">
    <ul class="nav navbar-nav">
      <li class="">
        <%= link_to "Inicio", root_path, :class => "navbar-link" %>  
      </li>
      <li class="">
        <% if(current_user.role != 'none') %>
          <%= link_to "Administrador", rails_admin_path, :class => "navbar-link" %>  
        <% end %>
      </li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Empleados <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to "Nuevo Empleado", new_employee_path, :class => "dropdown-item" %> </li>
        </ul>
      </li>
      <li>
        <%= link_to "Contratos", root_path, :class => "navbar-link" %>  
      </li>
    </ul>

    <ul class="nav navbar-nav pull-right">        

      <span>
        <% unless current_user.avatar.blank?%>                 
          <%= image_tag(current_user.avatar.url, alt:"avatar_menu", size:"40", :class=>'img-circle img-avatar-menu')%>
        <%else%>
          <%=image_tag("users/avatar_menu.png", alt:"avatar_menu", size:"40", :class=>'img-circle img-avatar-menu')%>  
        <%end%>
      </span>

      <strong>
        <%= current_user.last_name.capitalize %> <%= current_user.first_name.capitalize[0,1]%>.
      </strong>&nbsp;             
      <%= link_to destroy_user_session_path, method: :delete, title: 'Salir',   class: "btn btn-default btn_out" do %> 
        <i class="fa fa-sign-out" aria-hidden="true"></i>
      <% end %>
      <span></span>

    </ul>
  </div>
</nav>
